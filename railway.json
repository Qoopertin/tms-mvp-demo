{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install --no-dev --optimize-autoloader && npm install && npm run build"
    },
    "deploy": {
        "startCommand": "sh -c 'echo PRE-START; touch database/database.sqlite; chmod 777 database/database.sqlite; echo MIGRATING; php artisan migrate --force; echo STARTING; php artisan serve --host=0.0.0.0 --port=$PORT'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}